#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char at;	
	printf("Enter the character\n");
	scanf("%c", & at);

	printf("\n The ASCII value of %c is %d\n ", at,at);
    	return (0);
}
